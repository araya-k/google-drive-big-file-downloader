# Bash Script to download big file from Google Drive
How to use?
+ Simply run the .sh file via command line, and input the file URL when prompted
+ Adding it to bash profile would also save a lot of time (;
